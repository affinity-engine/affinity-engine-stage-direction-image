{{#ember-animation-box classNames="ae-hidden" animationAdapter=animationAdapter transitions=transitions resolve=(action resolve)}}
  {{affinity-engine-stage-direction-image-frame
    caption=caption
    fixtureId=directable.attrs.fixture.id
    imageCategory=imageCategory
    imageElement=imageElement
    src=src
    engineId=engineId
    windowId=windowId
  }}
{{/ember-animation-box}}
